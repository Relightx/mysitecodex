---
const logos = ['CLIENT 01', 'CLIENT 02', 'CLIENT 03', 'CLIENT 04', 'CLIENT 05'];
---

<section class="logo-strip" aria-label="Client logos">
  {logos.map((logo) => (
    <div class="logo-pill" aria-label={logo}>{logo}</div>
  ))}
</section>
